# vLLM Mac 第二阶段学习与性能基线

记录日期：2026-09-20

## 环境

- 设备：MacBook Air，Apple M5
- 统一内存：16GB
- 系统架构：macOS ARM64
- 推理后端：Apple Metal
- Python 环境：`vllm-lab/.venv`
- vLLM：0.29.0+cpu
- vLLM Metal：0.29.0
- 模型：`Qwen/Qwen3-0.6B`
- 服务地址：`http://127.0.0.1:8000`
- 最大上下文：2048 tokens
- `gpu-memory-utilization`：0.50
- 模型缓存：`vllm-lab/.cache/huggingface`

## 推理参数实验

### temperature

- `temperature=0` 时，多次生成结果稳定、基本一致。
- `temperature=1.0` 时，输出变化明显增多。
- 请求均正常完成，现象符合预期。

### top_p

- 固定 `temperature=0.8` 后，对比了较小和较大的 `top_p`。
- 较大的候选范围带来了更多输出变化。
- 请求均正常完成，现象符合预期。

### max_tokens

- 已理解 `max_tokens` 是输出 token 上限，不是字符数上限。
- 已理解 `finish_reason=length` 表示达到输出限制，`stop` 表示模型正常结束。
- 本项属于已有知识，未作为重点重复测试。

## Qwen3 思考模式实验

测试问题：一个数的两倍加 6 等于 20，这个数是多少？

| 模式 | prompt tokens | completion tokens | finish_reason | 结果 |
|---|---:|---:|---|---|
| 关闭思考 | 32 | 77 | `stop` | 直接给出答案 7 和解题过程 |
| 开启思考 | 28 | 512 | `length` | 输出 `<think>` 思考过程，最终答案部分被截断 |

结论：

- `enable_thinking=true` 已生效。
- 当前服务没有配置独立的 reasoning parser，因此思考过程位于 `content` 的 `<think>` 标签中，`reasoning` 字段为 `null`。
- 简单问题开启思考模式会显著增加 token 消耗和响应时间。
- 思考过程与最终答案共享 `max_tokens`，可能挤占最终答案空间。

## 单次流式请求

- TTFT：0.8166 秒
- 总响应时间：1.4849 秒
- 内容块数量：62
- `prompt_tokens`：24
- `completion_tokens`：63
- 首 token 后近似输出速度：92.77 tokens/s
- `finish_reason`：`stop`
- 正常收到 `[DONE]`：是

## 连续请求稳定性

- 预热请求：2 次，不计入正式统计
- 第一次预热 TTFT：0.7944 秒
- 第二次预热 TTFT：0.0340 秒
- 正式请求：20 次
- 成功请求：20/20
- 失败请求：0

| 指标 | 结果 |
|---|---:|
| TTFT 最小值 | 0.0224 秒 |
| TTFT 平均值 | 0.0234 秒 |
| TTFT P50 | 0.0233 秒 |
| TTFT P95 | 0.0247 秒 |
| TTFT 最大值 | 0.0249 秒 |
| 总响应时间 P50 | 0.6685 秒 |
| 总响应时间 P95 | 0.6768 秒 |
| 输出速度平均值 | 95.98 tokens/s |
| 输出速度 P50 | 96.06 tokens/s |

结论：

- 第一次请求存在明显预热开销。
- 预热后 TTFT 稳定在约 23 毫秒。
- 连续 20 次请求没有失败，单请求输出速度约为 96 tokens/s。
- 本实验重复使用相同提示词，结果可能同时包含 Metal 执行预热及缓存收益，不能据此单独证明某一种缓存机制生效。

## 并发压力测试

数据来源：用户选择不亲自执行后，由助手在第二阶段收尾时运行测试脚本采集。每个并发档位共发送 8 个请求，思考模式关闭。

| 并发数 | 成功请求 | 整批耗时 | TTFT P50 | TTFT P95 | 总时间 P50 | 总时间 P95 | 聚合吞吐量 |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 2 | 8/8 | 1.6292 秒 | 0.0377 秒 | 0.0454 秒 | 0.4062 秒 | 0.4161 秒 | 171.87 tokens/s |
| 4 | 8/8 | 0.8249 秒 | 0.0366 秒 | 0.0435 秒 | 0.4073 秒 | 0.4174 秒 | 339.44 tokens/s |
| 8 | 8/8 | 0.4087 秒 | 0.0411 秒 | 0.0419 秒 | 0.3532 秒 | 0.3888 秒 | 582.31 tokens/s |

通过 `/metrics` 以约 50 毫秒间隔采样：

- 最高 `Running`：8
- 最高 `Waiting`：0
- 非零负载样本：42
- 请求失败：0
- OOM 或引擎崩溃：未出现

解释：该模型和请求负载较小，8 个请求都能进入运行状态，因此没有观察到排队。聚合吞吐量随并发增加而提升；这组短请求结果只代表当前模型、提示词和配置，不能直接外推到更大模型或更长上下文。

## 第二阶段验收状态

| 项目 | 状态 |
|---|---|
| temperature | 完成 |
| top_p | 完成 |
| max_tokens 与截断 | 已掌握 |
| Qwen3 思考模式 | 完成 |
| 普通输出与流式输出 | 完成 |
| TTFT、总响应时间、tokens/s | 完成 |
| 连续请求稳定性 | 完成，20/20 成功 |
| Mac 单请求性能基线 | 完成 |
| 2、4、8 并发 | 助手收尾采集，全部成功 |
| Running/Waiting | 通过 `/metrics` 采集，最高 8/0 |

第二阶段结论：推理参数、思考模式、流式输出、单请求稳定性和并发基线均已收集完成，可以进入第三阶段。
